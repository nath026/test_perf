<form method="post" action="{{ path('movies_genres_delete', {'genre': movies_genre.genre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ movies_genre.genre) }}">
    <button class="btn">Delete</button>
</form>
