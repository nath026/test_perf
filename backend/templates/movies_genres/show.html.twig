{% extends 'base.html.twig' %}

{% block title %}MoviesGenres{% endblock %}

{% block body %}
    <h1>MoviesGenres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Genre</th>
                <td>{{ movies_genre.genre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('movies_genres_index') }}">back to list</a>

    <a href="{{ path('movies_genres_edit', {'genre': movies_genre.genre}) }}">edit</a>

    {{ include('movies_genres/_delete_form.html.twig') }}
{% endblock %}
