{% extends 'base.html.twig' %}

{% block title %}Directors{% endblock %}

{% block body %}
    <h1>Directors</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ director.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ director.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ director.lastName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('directors_index') }}">back to list</a>

    <a href="{{ path('directors_edit', {'id': director.id}) }}">edit</a>

    {{ include('directors/_delete_form.html.twig') }}
{% endblock %}
