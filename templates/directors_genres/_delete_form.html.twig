<form method="post" action="{{ path('directors_genres_delete', {'genre': directors_genre.genre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ directors_genre.genre) }}">
    <button class="btn">Delete</button>
</form>
