<form method="post" action="{{ path('roles_delete', {'actorId': role.actorId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role.actorId) }}">
    <button class="btn">Delete</button>
</form>
